header(class="navbar navbar-inverse navbar-fixed-top bs-docs-nav", role="banner")
  .container

  .navbar-header
    button(type="button", data-toggle="collapse", data-target="bs-navbar-collapse").navbar-toggle
    span.sr-only
      p
        | Toggle navigation
    span.icon-bar
    span.icon-bar
    span.icon-bar

    a.navbar-brand(href="#/")
      | Mark2Cure

  nav(role="navigation").collapse.navbar-collapse.bs-navbar-collapse
    ul.nav.navbar-nav
      //- if collection.completed().length ) { %>
    - if True
      li.dropdown
        a(href="#", data-toggle="dropdown").dropdown-toggle
          | Feedback b.caret

        ul.dropdown-menu
          li
            a(href="#/message")
              | Message
          li
            a(href="#/survey")
              | Survey

      li
        a(href="#").network
          img(src="#{STATIC_URL}images/network_icon.png")
    - else
      li
        a(href="#/instructions")
          | Instructions

      li.dropdown
        a(href="#", data-toggle="dropdown").dropdown-toggle
          | username b.caret

    // - if( collection.completed().length ) { %>
    //- if True
      ul.dropdown-menu
        li
          a(href="#/settings")
            | Settings
        li
          a(href="/logout")
            | Logout
        li.divider
        li
          a(href="#/instructions")
            | Instructions
