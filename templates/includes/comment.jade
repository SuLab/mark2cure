div(id="comment", tabindex="-1", role="dialog", aria-labelledby="myModalLabel", aria-hidden="true").modal.fade
  // Just like refute but not section specific
  .modal-dialog
    .modal-content
      a(data-dismiss="modal").close-btn
        | &times;

      .modal-header
        h3
          | Was there a problem with our answers?

      .modal-body
        form(action="/document/{{ doc.pk }}/comment/create/", method="post", role="form").form-horizontal.form-via-ajax
          {% csrf_token %}
          .form-group
            .col-sm-12
              textarea#new_message(name="message", placeholder="Type in your message", rows="5").form-control

          .form-group
            .col-sm-6.col-sm-offset-3
              button(type="submit").btn.btn-block.btn-success
                | Let us know!

      .modal-footer
        button(data-dismiss="modal").btn.btn-warning
          | Close


