extends base

block header

block content
  .container
    .row
        .col-md-6.col-md-offset-3
            h2.text-center Forgot Your Password?
            form(method="post")
              {% csrf_token %}
              #{form.email.label_tag}
              #{form.email}
              input(type="submit", value="Email Reset Link").btn.btn-default
