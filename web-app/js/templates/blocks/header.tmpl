<div class="navbar-inner">
  <div class="container">

    <% if( !first_run && collection.completed().length ) { %>
      <a class="brand" href="#/library#">Mark2Cure</a>
    <% } else { %>
      <p class="brand">Mark2Cure</p>
    <% } %>

      <ul class="nav navbar-nav">
        <% if( !first_run && collection.completed().length ) { %>
          <li><a href="#/library">Library</a></li>
          <li><a href="#/message">Message</a></li>
          <li><a href="#/feedback">Feedback</a></li>
          <li><a href="#" class="network"><span class="icon-tasks"></span></a></li>
        <% } %>

        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= username %> <b class="caret"></b></a>
          <% if( !first_run && collection.completed().length ) { %>
            <ul class="dropdown-menu">
              <li><a href="#/settings">Settings</a></li>
              <li class="divider"></li>
              <li><a href="#" class="complete">Complete</a></li>
              <li><a href="#" class="welcome">Welcome</a></li>
            </ul>
          <% } %>
        </li>
      </ul>
  </div>
</div>
