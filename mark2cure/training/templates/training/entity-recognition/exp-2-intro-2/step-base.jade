extends base

block header
  - with headerMessage='Training 2 - Learning a new concept: Genes'
    include includes/header

block content
  #Training2Modal(tabindex="-1", role="dialog", aria-labelledby="myModalLabel", aria-hidden="true").modal.fade
    .modal-dialog
      .modal-content
        .modal-header
          h4.modal-title.text-xs-center The Genes Concept includes more than just gene names
          p.card-text.text-xs-center <strong>Do NOT mark Diseases Concept terms in this tutorial.</strong>

        .modal-body

          .card
            .card-header
              h4.card-title.text-xs-center Acquire the Gene Marking Skill by learning the Genes Concept.

            .card-block
              p.text-xs-center The Genes Concept includes:
              ul.list-unstyled.list-inline.text-xs-center
                li.list-inline-item <strong>Genes</strong>
                  ul.list-unstyled
                    li Gene Names
                    li Gene Symbols
                li.list-inline-item <strong>Gene Products</strong>
                  ul.list-unstyled
                    li Proteins
                    li MicroRNAs

        .modal-footer
          button(type="button", data-dismiss="modal").btn.btn-primary Close

  .container
    .row.m-t-1
      .col-md-12
        block step-instructions
          h2 A <strong>disease</strong> is a particular abnormal, pathological condition that affects part or all of an organism
          p In the box below, mark every instance of the disease (rule #1)

    #partner(style='display: none;').row.m-t-1
      .row
        .col-md-4.col-md-offset-4
          .alert.alert-success
            h2.text-xs-center Good job!

    .row.m-t-1
      #quest-alerts.col-md-10.col-md-offset-1
      #quest.col-md-10.col-md-offset-1.paragraph-box
        block paragraph

    .row.m-t-1
      .col-md-8.col-md-offset-2
        .progress
          block progress

      .col-md-8.col-md-offset-2
        #next-container.pull-right
          block next-link

block post-footer
  include training/entity-recognition/includes/intro-1-js

  block answers

  script.
      if(ga) { ga('send', 'event', 'ER Training', 'start', key); }
      var validation_check = function(collection) {
        if(validate(collection, answers)) { correct(); }
      };
      YPet.AnnotationTypes = new AnnotationTypeList([{name: 'Genes', color: '#B1FFA8'}]);
